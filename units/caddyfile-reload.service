[Unit]
Description=My Service Restarter
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/bin/podman exec systemd-caddy caddy reload -c /etc/caddy/Caddyfile

[Install]
WantedBy=multi-user.target